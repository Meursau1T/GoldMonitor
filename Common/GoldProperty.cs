namespace GoldMonitor.Common;

public static class GoldProperty {
    
    public enum Currency {
        CNY,
        USD
    }

    public enum Unit {
        OZ,
        G
    }

    public enum Locale {
        EN,
        ZH
    }
}