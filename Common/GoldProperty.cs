namespace GoldMonitor.Common;

public class GoldProperty {
    
    public enum Currency {
        CNY,
        USD
    }

    public enum Unit {
        OZ,
        G
    }

    public enum Locale {
        EN,
        ZH
    }
}